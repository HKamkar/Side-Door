# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
Desktop.ini

# Linux
*~
.directory

# Editors
.vscode/
.idea/
*.swp
*.swo

# Claude Code per-user settings (project-level shared settings can be committed)
.claude/settings.local.json

# Python (in case anyone adds scripts later)
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Node
node_modules/

# User data — side-door writes here when installed; should never be in the plugin source
side-door-workspace/
applications.csv
profile.md
preferences.md
application-data.md
shortlist.md
network-matches.md
Connections.csv
# Anchored to repo root: these are workspace subfolder names that also collide
# with skill/reference paths (e.g. skills/interview-prep/) if left unanchored.
/jobs/
/postings/
/patterns/
/interview-prep/

# Build artifacts — dev builds excluded, the bundled release artifact is allowed
*.plugin
!side-door.plugin
*.zip
/tmp/
build/
dist/
